import { styled } from '../../stitches.config';

export const EmptyCartMessageWrapper = styled('div', {
	mx: 'auto',
	my: '5rem',
	textAlign: 'center',

	p: {
		fontSize: '$26',
		color: '$primaryOrange',
		my: '3rem'
	}
});
